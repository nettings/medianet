#!/bin/bash

. /medianet/lib/mn_includes

OLDPWD=`pwd`
cd /medianet
mv "$CONF_FILE_PERMISSIONS" "$CONF_FILE_PERMISSIONS".old
"$FIND" overlay boot ! -type l ! -name "*~" -exec "$STAT" -c "%N %U:%G %a" {} \; >> "$CONF_FILE_PERMISSIONS"
cd "$OLDPWD"
