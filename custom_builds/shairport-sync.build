autoreconf -i -f
./configure \
	--with-ssl=openssl \
	--with-stdout \
	--with-alsa \
	--with-jack \
	--with-avahi \
	--with-soxr \
	--with-metadata \
	--sysconfdir=/etc/medianet
make -j4
sudo DESTDIR="/medianet/overlay/" make install
