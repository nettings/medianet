#!/bin/bash

BASEDIR=`dirname $0`
. "$BASEDIR"/mn_includes

OLDPWD=`pwd`
cd "$BASEDIR/../custom_builds"
find "$BASEDIR" -name "mn_checkout.*" ! -name "*~" | while read line ; do
  scream "Running "`basename $line`"..."
  . $line && success || failure
done
