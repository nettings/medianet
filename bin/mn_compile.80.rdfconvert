
TARGET=/medianet/overlay/usr/local/bin/rdfconvert
sudo cp rdfconvert.py /usr/local/lib/

sudo cat << EOF > "$TARGET" 
#!/bin/bash
python3 /usr/local/lib/rdfconvert.py $@


EOF

sudo chmod +x "$TARGET"
