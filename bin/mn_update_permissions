#!/bin/bash

FILE="permissions.medianet"
OLDPWD=`pwd`
cd /medianet
mv "$FILE" "$FILE".old
find overlay boot ! -type l ! -name "*~" -exec stat -c "%N %U:%G %a" {} \; >>permissions.medianet
cd "$OLDPWD"
